// Crea un objeto 'persona' con propiedades: nombre, edad y profesion
let persona = {
    nombre : "gerardo", 
    edad : 22,
    profesion : "inge"
}

// Imprime en consola el valor de la propiedad 'nombre' del objeto persona
console.log(persona.nombre);