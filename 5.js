// Crea una función llamada saludar que recibe un nombre como parámetro
function saludar(nombre) {
    // Imprime la concatenación de un saludo y el nombre recibido como parátro al llamar la función. 
    console.log("hola " + nombre);
}

// Llama la función saludar enviando "airam" como parámetro
saludar("airam");