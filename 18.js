// Crea un arreglo con una lista de palabras
let palabras = ["banana", "pera", "uva", "manzana"];

// Ordena el arreglo de palabras en orden alfabético y lo guarda en ordenadas
let ordenadas = palabras.sort();
// Imprime en consola el arreglo ordenadas
console.log(ordenadas);
