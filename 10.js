// Crea un arreglo edades con varios valores numéricos
let edades = [12, 18, 25, 10, 30, 16];

// Filtra el arreglo edades para obtener solo aquellos valores que son mayores o iguales a 18,
// y almacena el resultado en la variable llamada mayores
let mayores = edades.filter((edad) => edad >= 18 );

// Imprime el arreglo mayores en consola.
console.log(mayores);
