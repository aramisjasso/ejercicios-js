// Crea un arreglo de números
let numeros = [2, 4, 6, 8, 10];

// Recorre el arreglo de números con la función forEach()
numeros.forEach(
    // Imprime cada elemento en consola
    (element) => console.log(element)
);
